name: '138 - Mesa Easy Diffusion C++ [RESERVED]'
on:
  workflow_dispatch:
    inputs:
      platform: {description: Platform, required: true, default: linux, type: choice, options: [linux, windows]}
permissions: {contents: read}
jobs:
  reserved:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Reserved index 138 for future Mesa Easy Diffusion C++ compilation.'
