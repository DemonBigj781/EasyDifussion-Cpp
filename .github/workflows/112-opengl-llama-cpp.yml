name: '112 - OpenGL llama.cpp [RESERVED]'
on:
  workflow_dispatch:
    inputs:
      platform: {description: Platform, required: true, default: linux, type: choice, options: [linux, windows]}
permissions:
  contents: read
jobs:
  reserved:
    runs-on: ubuntu-24.04
    steps:
      - name: Reserved slot
        run: echo 'Reserved index 112 for future OpenGL llama.cpp compilation.'
