name: '129 - DirectML Complete Compile [RESERVED]'
on:
  workflow_dispatch:
    inputs:
      mode: {description: Mode, required: true, default: check, type: choice, options: [check, compile]}
permissions: {contents: read}
jobs:
  reserved:
    runs-on: windows-2025
    steps:
      - uses: actions/checkout@v7
      - shell: pwsh
        run: Write-Host '129 is reserved for the DirectML complete compile after DirectML API.cpp, llama.cpp, and Easy Diffusion C++ are wired.'
