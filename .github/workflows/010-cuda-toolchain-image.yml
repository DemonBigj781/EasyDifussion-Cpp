name: '010 - CUDA Toolchain Image [RESERVED]'
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  reserved:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Reserved index 010 for CUDA toolchain/image construction.'
