name: '100 - Vulkan Toolchain Image [RESERVED]'
on:
  workflow_dispatch: {}
permissions:
  contents: read
jobs:
  reserved:
    runs-on: ubuntu-24.04
    steps:
      - name: Reserved slot
        run: echo 'Reserved index 100 for Vulkan toolchain/image construction.'
