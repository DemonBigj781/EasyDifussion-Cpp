name: '120 - DirectML Toolchain Image [RESERVED]'
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  reserved:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Reserved index 120 for future DirectML toolchain image construction.'
