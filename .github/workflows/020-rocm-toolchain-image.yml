name: '020 - ROCm Toolchain Image [RESERVED]'
on:
  workflow_dispatch:
permissions: {contents: read}
jobs:
  reserved:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Reserved index 020 for ROCm toolchain/image construction.'
