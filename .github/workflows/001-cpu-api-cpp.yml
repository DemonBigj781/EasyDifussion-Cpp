name: '001 - CPU API.cpp'

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  branch-note:
    runs-on: ubuntu-24.04
    steps:
      - name: Main branch note
        run: echo 'CPU API.cpp is active on the Theory branch. Select Theory when dispatching this workflow for the current CPU API/xFormers compile path.'
