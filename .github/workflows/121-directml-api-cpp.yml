name: '121 - DirectML API.cpp [RESERVED]'
on:
  workflow_dispatch: {}
permissions:
  contents: read
jobs:
  reserved:
    runs-on: windows-2025
    steps:
      - name: Reserved slot
        shell: pwsh
        run: Write-Host 'Reserved index 121 for future DirectML API.cpp compilation.'
