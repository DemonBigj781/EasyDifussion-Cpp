name: '121 - DirectML API.cpp [RESERVED]'
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  reserved:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Reserved index 121 for future DirectML API.cpp compilation.'
