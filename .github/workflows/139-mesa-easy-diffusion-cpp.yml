name: '139 - Mesa Complete Compile [RESERVED]'
on:
  workflow_dispatch:
    inputs:
      mode: {description: Mode, required: true, default: check, type: choice, options: [check, compile]}
permissions: {contents: read}
jobs:
  reserved:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
      - run: echo '139 is reserved for the Mesa complete compile after Mesa API.cpp, llama.cpp, and Easy Diffusion C++ are wired.'
