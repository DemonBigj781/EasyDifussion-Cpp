name: '110 - OpenGL Toolchain Image [RESERVED]'
on:
  workflow_dispatch:
permissions: {contents: read}
jobs:
  reserved:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Reserved index 110 for OpenGL toolchain/image construction.'
