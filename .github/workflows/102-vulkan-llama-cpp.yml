name: '102 - Vulkan llama.cpp [RESERVED]'
on:
  workflow_dispatch:
    inputs:
      platform: {description: Platform, required: true, default: linux, type: choice, options: [linux, windows]}
permissions:
  contents: read
jobs:
  reserved:
    runs-on: ubuntu-24.04
    steps:
      - name: Reserved slot
        run: echo 'Reserved index 102 for future Vulkan llama.cpp compilation.'
