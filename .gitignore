# Installed runtimes and generated backend binaries
/installer_files/
/backends/
/.venv/

# Models live in a separately managed shared tree
/models

# Generated images, caches, logs, and temporary state
/bucket/
/tmp/
/scripts/install_status.txt
**/__pycache__/
*.py[cod]
*.log
**/.thumbnails/

# Deployment backups and local configuration snapshots
/backups/
/config.yaml.bak
/config.yaml.pre-*
*.pre-*
/options.json
/source/artifacts
# Keep the upstream/source checkout as its own Git repository
/source
