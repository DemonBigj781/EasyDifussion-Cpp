# GGML feature scaffold

Status: **future-only scaffold**.

This directory reserves a possible normalized DiffUser.cpp boundary for later GGML
work. It has no method contract, build references, implementation, or support
claim. It does not replace either vendored GGML tree or the live
stable-diffusion.cpp attention compatibility adapters.

Before implementation, reconcile its scope with the
[shared-GGML plan](../../../../docs/todo/32-merge-llama-stable-diffusion.md),
especially the shared-baseline, ABI, symbol, registry, and lifecycle decisions.
See [the layout audit](../../LAYOUT_AUDIT.md).
