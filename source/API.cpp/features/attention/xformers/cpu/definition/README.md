# CPU xFormers definitions

Structure-only placeholder for CPU-specific xFormers translations. Files in this directory will later translate CPU-native behavior into the normalized method semantics consumed by `common/`.

No functional definition is implemented yet.
