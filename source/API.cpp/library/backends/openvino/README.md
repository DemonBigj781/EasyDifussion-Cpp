# OpenVINO backend

The Library exposes OpenVINO device discovery through `OpenvinoHandler`.
OpenVINO CPU, GPU, and NPU enumeration remains in `features/detect/openvino/`.
Workflow `051` provides compiler-only coverage.
