# Mesa backend

The Library exposes current-context Mesa renderer discovery through
`MesaHandler`, distinguishing software CPU renderers from hardware GPU
renderers. Workflow `131` provides compiler-only coverage.
