# OpenCL backend

The Library exposes OpenCL device discovery through `OpenclHandler`. Native
OpenCL CPU, GPU, and accelerator calls remain in `features/detect/opencl/`.
Workflow `041` provides compiler-only coverage.
