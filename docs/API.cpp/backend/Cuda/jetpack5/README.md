# JetPack 5 CUDA variant

JetPack 5 is tracked separately from desktop/server CUDA because its CUDA toolkit, Jetson platform libraries, compiler environment, architecture targets, and deployment constraints are tied to the JetPack/L4T stack.

Version-specific attention and cache documentation belongs under `versions/` for this variant.
