# JetPack 6 CUDA variant

JetPack 6 is tracked separately from desktop/server CUDA and JetPack 5 because its CUDA toolkit, Jetson platform libraries, compiler environment, architecture targets, and deployment constraints are tied to the JetPack 6/L4T stack.

Version-specific attention and cache documentation belongs under `versions/` for this variant.
